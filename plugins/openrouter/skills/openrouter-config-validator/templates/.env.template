# OpenRouter API Configuration Template
# Copy this file to .env and fill in your values

# ============================================================================
# REQUIRED CONFIGURATION
# ============================================================================

# Your OpenRouter API key (get from https://openrouter.ai/keys)
# Format: sk-or-v1-{64 hex characters}
OPENROUTER_API_KEY=sk-or-v1-your-api-key-here

# ============================================================================
# MODEL CONFIGURATION
# ============================================================================

# Primary model to use (see https://openrouter.ai/models)
# Examples:
#   - anthropic/claude-4.5-sonnet
#   - openai/gpt-4-turbo
#   - meta-llama/llama-3-70b-instruct
OPENROUTER_MODEL=anthropic/claude-4.5-sonnet

# Fallback models (comma-separated, tried in order if primary fails)
# These will be used if the primary model is unavailable or rate limited
OPENROUTER_FALLBACK_MODELS=anthropic/claude-4.5-sonnet,openai/gpt-3.5-turbo

# ============================================================================
# MONITORING CONFIGURATION
# ============================================================================

# Application name for usage tracking (visible in OpenRouter dashboard)
# This helps you identify requests in the usage dashboard
OPENROUTER_APP_TITLE=My Application

# Site URL for request attribution
# Used with HTTP-Referer header for tracking
OPENROUTER_SITE_URL=https://myapp.example.com

# ============================================================================
# ROUTING PREFERENCES
# ============================================================================

# Provider preferences (comma-separated order of provider preference)
# Examples: OpenAI,Anthropic,Google
# Leave empty to use OpenRouter's automatic routing
OPENROUTER_PROVIDER_PREFERENCES=

# ============================================================================
# RATE LIMITING & BUDGET
# ============================================================================

# Maximum requests per minute (optional, for client-side rate limiting)
OPENROUTER_MAX_REQUESTS_PER_MINUTE=60

# Budget alert threshold in USD (optional, for client-side tracking)
OPENROUTER_BUDGET_ALERT_THRESHOLD=100.00

# ============================================================================
# ADVANCED CONFIGURATION
# ============================================================================

# API base URL (usually no need to change)
OPENROUTER_BASE_URL=https://openrouter.ai/api/v1

# Request timeout in seconds
OPENROUTER_TIMEOUT=30

# Retry attempts for failed requests
OPENROUTER_MAX_RETRIES=3

# ============================================================================
# NOTES
# ============================================================================
#
# Security:
# - Never commit this file to version control
# - Add .env to your .gitignore
# - Set file permissions: chmod 600 .env
#
# Model Selection:
# - Browse available models: https://openrouter.ai/models
# - Check pricing: Different models have different costs
# - Test models before production use
#
# Monitoring:
# - Enable X-Title and HTTP-Referer for usage tracking
# - View usage dashboard: https://openrouter.ai/settings/usage
# - Set up budget alerts in the dashboard
#
# Fallback Configuration:
# - Order models from most preferred to least
# - Include at least one highly available model
# - Balance cost vs. quality in fallback chain
#
